# ===========================================================================
# 저작권 있는 게임 데이터 — 저장소에 절대 올리지 않는다.
# 이 프로젝트는 원본 ROM을 읽기 전용 입력으로만 취급한다.
# ===========================================================================
*.gba
*.gb
*.gbc
*.nds
*.sav
*.srm
*.ss1
*.ss2
*.mss
PC판 패치/

# ---------------------------------------------------------------------------
# 로컬 파생 ROM과 미리보기 산출물 (원본 ROM에서 재생성 가능)
# ---------------------------------------------------------------------------
poc/output/
poc/archive/

# ---------------------------------------------------------------------------
# 런타임 검증 로그 덤프 — 용량이 크고 재생성 가능하다
# ---------------------------------------------------------------------------
**/stdout.txt
**/stderr.txt

# ---------------------------------------------------------------------------
# 임시 패키징 작업 폴더 (dist/ 의 최종 산출물만 추적한다)
# ---------------------------------------------------------------------------
analysis/test-patch-*/
analysis/exe2-v0-9-*/

# ---------------------------------------------------------------------------
# 서드파티 도구 바이너리 — 업스트림에서 직접 받는다
#   TextPet : https://github.com/Prof9/TextPet
#   Flips   : https://github.com/Alcaro/Flips
# ---------------------------------------------------------------------------
external/

# ---------------------------------------------------------------------------
# 빌드·캐시 부산물
# ---------------------------------------------------------------------------
__pycache__/
*.pyc
*.pyo
*.tmp
Thumbs.db
desktop.ini

# ---------------------------------------------------------------------------
# 임시 패키징·검증 작업 폴더 (배포본은 GitHub Releases 로 올린다)
# ---------------------------------------------------------------------------
analysis/*-package-*/
analysis/package_check*/
analysis/*.previous.*.zip
dist/
